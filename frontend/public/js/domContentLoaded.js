import { handleFileInputChange } from "./fileInputHandler";
import { handleFormSubmit } from "./formSubmitHandler";

document.addEventListener('DOMContentLoaded', () => {
  handleFileInputChange();
  handleFormSubmit();
});
